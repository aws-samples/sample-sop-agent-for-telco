apiVersion: v1
kind: Service
metadata:
  name: {{ include "anda.fullname" . }}
  labels:
    {{- include "anda.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anda.selectorLabels" . | nindent 4 }}
