apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "anda.fullname" . }}-config
  labels:
    {{- include "anda.labels" . | nindent 4 }}
data:
  agent-config.yaml: |
    {{- include "anra-common.agent-config" . | nindent 4 }}
