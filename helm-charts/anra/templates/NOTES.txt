ANRA deployed to namespace {{ .Release.Namespace }}.

Dashboard:
{{- if .Values.ingress.enabled }}
  Ingress (ALB):  kubectl get ingress {{ include "anra.fullname" . }} -n {{ .Release.Namespace }} -o jsonpath='{.status.loadBalancer.ingress[0].hostname}{"\n"}'
  When ready, use the ALB hostname (optionally in front of CloudFront) — see docs/CLOUDFRONT.md
{{- else if eq .Values.webui.service.type "NodePort" }}
  http://<NODE_IP>:{{ .Values.webui.service.nodePort }}
{{- else }}
  kubectl port-forward svc/anra {{ .Values.webui.port }}:{{ .Values.webui.port }} -n {{ .Release.Namespace }}
{{- end }}

Monitoring:
  InfluxDB: {{ include "anra.influxdbUrl" . | default "not configured" }}
  Alertmanager: {{ include "anra.alertmanagerUrl" . | default "not configured" }}

To deploy sample workloads (Open5GS, srsRAN, Telegraf, InfluxDB):
  See configs/ directory or run: deploy.sh --with-sample-workload --with-monitoring
