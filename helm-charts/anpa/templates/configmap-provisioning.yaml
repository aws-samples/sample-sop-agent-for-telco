apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "anpa.fullname" . }}-provisioning
  labels:
    {{- include "anpa.labels" . | nindent 4 }}
data:
  agent-config.yaml: |
    {{- include "anra-common.agent-config" . | nindent 4 }}
